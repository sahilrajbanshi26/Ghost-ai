Read `AGENTS.md` before starting.

we are add the design system and UI primitive components.

Install and configure `shadcn/ui`.

Add this shndcn components:

-Button
-Card 
-Dialog 
-Input 
-Tabs
-Textarea
-ScorallArea

Do not madify the gererated `component/ui/*` files after installation.

Also install `lucide-react`.

creat `lib/utils.ts` with a reusable `cn()` helper for merging tailwind classes.


Ensure all the component match the existing dark theme in `globals.css`.

###  check when done 

- All components  import without erorrs 
- `cn()` works properly
- no default ligth styling appears
